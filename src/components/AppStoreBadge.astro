---
import { appleHref } from "../lib/stores";

interface Props {
  size?: "lg" | "md" | "sm";
  utm?: string;
  /** Tag this link as a conversion CTA (adds data-aw-cta for gtag tracking). */
  cta?: boolean;
}

const { size = "lg", utm = "kalum-web", cta = false } = Astro.props;

// Sized to match GooglePlayBadge's apparent height at each size.
// Apple badge aspect ratio ≈ 2.99:1, Google Play ≈ 2.58:1.
// lg widths chosen so heights line up with GooglePlayBadge w-56/w-48/w-40.
const widthClass =
  size === "lg" ? "w-[260px]" : size === "md" ? "w-[222px]" : "w-[185px]";

const href = appleHref(utm);
---

<a
  href={href}
  rel="noopener"
  aria-label="Download Kalum on the App Store"
  class="inline-block transition hover:scale-[1.02]"
  data-aw-cta={cta ? "" : undefined}
>
  <img
    src="/badges/app-store.svg"
    alt="Download on the App Store"
    width="119.66"
    height="40"
    class={`h-auto ${widthClass}`}
    loading="eager"
    decoding="async"
  />
</a>
