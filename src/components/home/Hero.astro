---
import { Image } from "astro:assets";
import dialer from "../../assets/screenshots/dialer.png";
import GooglePlayBadge from "../GooglePlayBadge.astro";
---

<section class="brand-gradient relative overflow-hidden text-white">
  <div class="absolute inset-0 opacity-[0.07]" aria-hidden="true">
    <div class="absolute -top-32 -left-32 h-96 w-96 rounded-full bg-white blur-3xl"></div>
    <div class="absolute bottom-0 right-0 h-96 w-96 rounded-full bg-[var(--color-teal)] blur-3xl"></div>
  </div>

  <div class="relative mx-auto grid max-w-6xl gap-12 px-5 py-20 md:grid-cols-[1.15fr_1fr] md:gap-10 md:px-8 md:py-28 lg:py-32">
    <div class="flex flex-col justify-center">
      <p class="text-xs font-semibold uppercase tracking-[0.22em] text-[var(--color-teal-light)]">
        For the Middle Eastern diaspora
      </p>
      <h1 class="mt-5 text-4xl font-extrabold leading-[1.05] tracking-tight sm:text-5xl lg:text-6xl">
        Call family back home.
        <span class="block text-white/85">They just pick up the phone.</span>
      </h1>
      <p class="mt-6 max-w-xl text-lg leading-relaxed text-white/85 sm:text-xl">
        Cheap calls to Lebanon, Iraq, Jordan, Kuwait and 180+ countries.
        No app on their side. No internet on their side.
        Just a regular phone call.
      </p>

      <div class="mt-9 flex flex-col gap-4 sm:flex-row sm:items-center">
        <GooglePlayBadge size="lg" utm="kalum-web-hero" />
        <a
          href="#ios-waitlist"
          class="text-sm font-medium text-white/85 underline-offset-4 hover:text-white hover:underline"
        >
          iPhone? Get notified when iOS launches &rarr;
        </a>
      </div>

      <p class="mt-5 text-sm text-white/70">
        Start with $2. No subscription. Credit never expires.
      </p>
    </div>

    <div class="relative flex items-center justify-center md:justify-end">
      <div class="relative">
        <div
          class="absolute -inset-4 rounded-[2.5rem] bg-black/30 blur-2xl"
          aria-hidden="true"
        ></div>
        <div class="relative rounded-[2.2rem] border-[10px] border-black/80 bg-black shadow-2xl ring-1 ring-white/10">
          <Image
            src={dialer}
            alt="Kalum dialer screen showing $47.50 balance and a 10-digit phone number"
            width={320}
            height={693}
            class="h-auto w-[260px] rounded-[1.6rem] sm:w-[300px]"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </div>
</section>
