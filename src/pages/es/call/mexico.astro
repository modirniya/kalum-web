---
import DestinationEs from "../_DestinationEs.astro";

// Spanish destination page. All copy lives in ../_destinations-es.ts and the
// rate comes from the live card at build time — see ../_DestinationEs.astro.
//
// This was the one hand-written page in the subtree until 2026-09-19. It was
// folded into the shared template after the same two fixes had to be applied
// to it separately from the other four.
---

<DestinationEs slug="mexico" />
