{
  "_note": "Dial code → the date that destination's per-minute rate last changed, maintained by .github/workflows/refresh-rates.yml. Drives sitemap lastmod: a page's date moves only when its own rate actually moved, so a weekly redeploy that changes nothing stays quiet. Keys are E.164 dial codes (not slugs) so the refresh script never has to read destinations.ts. Entries below _note only."
}
